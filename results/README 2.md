# results/

lm-eval outputs, error-analysis reports, the headline table, and the inference
cost JSON land here. Large JSON/JSONL sample logs are gitignored (regenerated by
re-running eval); the small curated summaries (`headline_table.md`,
`error_analysis_*.md`, `inference_cost.json`) are kept.

Until the real Colab T4 runs complete, every metric here is **PENDING RUN**.
